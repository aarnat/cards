/* Notes:
 *  inherits from pile class
 *  Variables:
 *
 *  Methods:
 *      - public "random get"
 *      - public "sort"
 *          - note that we can sort in O(n) by going top to bottom in the deck
 *          array. It would be in suit-value order.
 *      - public override "topdeck", "untopdeck" = disable
 */


